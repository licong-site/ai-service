# Cloudflare Worker 本地开发环境变量
# 此文件仅用于本地开发，不会被提交到 Git

# DeepSeek API 密钥（必需）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# 允许的域名（可选，本地开发建议设置为 * 或包含 localhost）
ALLOWED_ORIGINS=*

# 或者设置具体的本地开发域名
# ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3000,http://127.0.0.1:3000